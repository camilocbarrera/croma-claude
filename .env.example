# Copia este archivo a `.env.local` y rellena las claves.
# `.env.local` está en .gitignore y NUNCA se sube al repositorio.

# Requerida: modelo (Groq, openai/gpt-oss-120b). Clave gratis en https://console.groq.com/keys
GROQ_API_KEY=

# Requerida para las búsquedas en la Rama Judicial (app/tools.ts).
# Consigue tu clave en https://usecroma.com (docs: https://docs.usecroma.com)
CROMA_API_KEY=
